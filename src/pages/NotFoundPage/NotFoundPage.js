import Typography from '@mui/material/Typography';

function NotFoundPage() {
  return (
    <Typography variant="h2" align="center">Page was not found</Typography>
  );
}

export default NotFoundPage;
